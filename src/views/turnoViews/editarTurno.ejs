<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Reservar Turno</title>
    <link rel="stylesheet" href="/css/turnos/editarTurno.css">
    <link rel="icon" href="/img/consultorio.png" type="image/png">
</head>
<body>
    <h1>Reservar Turno</h1>

    <!-- Formulario para ingresar el DNI y buscar al paciente -->
    <form action="/turno/editar/<%= turno.ID_Turno %>" method="POST">
        
        <!-- Ingreso del DNI del Paciente -->
        <label for="Dni_Paciente">DNI del Paciente:</label>
        <input type="text" name="Dni_Paciente" value="<%= turno.Dni_Paciente || '' %>" required>
        <button type="submit">Buscar Paciente</button>

        <!-- Si ya se ha encontrado al paciente, mostrar los datos -->
        <% if (turno.Nombre_Paciente) { %>
            <label for="Nombre_Paciente">Nombre del Paciente:</label>
            <input type="text" name="Nombre_Paciente" value="<%= turno.Nombre_Paciente %>" disabled>

            <label for="Apellido_Paciente">Nombre del Paciente:</label>
            <input type="text" name="Apellido_Paciente" value="<%= turno.Apellido_Paciente %>" disabled>

            <label for="Obra_Social">Obra Social:</label>
            <input type="text" name="Obra_Social" value="<%= turno.Obra_Social %>" disabled>

            <label for="Email_Paciente">Email del Paciente:</label>
            <input type="email" name="Email_Paciente" value="<%= turno.Email_Paciente %>" disabled>

            <label for="Motivo_Consulta">Motivo de Consulta:</label>
            <input type="text" name="Motivo_Consulta" value="<%= turno.Motivo_Consulta %>" required>

            <label for="Clasificacion">Clasificación:</label>
            <input type="text" name="Clasificacion" value="<%= turno.Clasificacion %>">
            
            <button type="submit">Reservar Turno</button>
        <% } %>
    </form>

</body>
</html>
