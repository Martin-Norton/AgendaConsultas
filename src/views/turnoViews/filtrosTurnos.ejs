<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Filtrar Turnos</title>
    <link rel="stylesheet" href="/css/turnos/filtrosTurnos.css">
    <link rel="icon" href="/img/consultorio.png" type="image/png">
</head>
<body>
    <h1>Filtrar Turnos Disponibles</h1>
    <form action="/turno/listarTurnos" method="POST">
        <!-- Seleccionar Profesional -->
        <label for="ID_Profesional">Para qué profesional es:</label>
        <select name="ID_Profesional" required>
            <% profesionales.forEach(profesional => { %>
                <option value="<%= profesional.ID_Profesional %>">
                    <%= profesional.Nombre_Profesional %> <%= profesional.Apellido_Profesional %>
                </option>
            <% }); %>
        </select>

        <!-- Seleccionar Especialidad -->
        <label for="ID_Especialidad">A qué especialidad corresponde:</label>
        <select name="ID_Especialidad" required>
            <% especialidades.forEach(especialidad => { %>
                <option value="<%= especialidad.ID_Especialidad %>">
                    <%= especialidad.Nombre_especialidad %>
                </option>
            <% }); %>
        </select>

        <!-- Otros filtros -->
        <label for="fechaInicio">Fecha Inicio:</label>
        <input type="date" name="fechaInicio">

        <label for="fechaFin">Fecha Fin:</label>
        <input type="date" name="fechaFin">

        <label for="horario">Horario (ej. 09:00):</label>
        <input type="time" name="horario">

        <button type="submit">Buscar Turnos</button>
        
    </form>

    <a href="/turno/reservados/filtros">Filtrar Turnos Reservados</a>
    <a href="/">Volver al Inicio</a>
</body>
</html>

