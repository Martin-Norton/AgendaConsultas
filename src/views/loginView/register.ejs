
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/login/register.css">
    <link rel="icon" href="/img/consultorio.png" type="image/png">
    <title>Registro</title>
</head>
<body>
    <div class="register-form">
        <h1>Formulario De Registro</h1>
        <form action="/register/this" method="POST">

            <label for="user">Usuario</label>
            <input type="text" name="user" id="user" required tabindex="1" autofocus>  

            <label for="name">Nombre Completo</label>
            <input type="text" name="name" id="name" required tabindex="2">

            <label for="rol">Rol</label>                          
            <select id="rol" name="rol" class="select-css" required tabindex="3">
                <option value="admin" selected>Admin</option>
                <option value="Paciente">Paciente</option>
                <option value="Scretaria">Seccretaria</option>                     
            </select>

            <label for="pass">Contraseña</label>
            <input type="password" name="pass" id="pass" tabindex="4">

            <div style="display: inline;">             
                <a href="/" class="btn-cancel">Cancelar</a>
                <input type="submit" class="btn-save" value="Guardar">           
            </div>

        </form>      
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>

    <% if (typeof alert !== "undefined" && alert) { %>
    <script>
        Swal.fire({
            title: '<%= alertTitle %>',
            text: '<%= alertMessage %>',
            icon: '<%= alertIcon %>',
            showConfirmButton: <%= showConfirmButton ? "true" : "false" %>,
            timer: <%= typeof timer !== "undefined" ? timer : "null" %>
        }).then(() => {
            window.location = '/login/this<%= ruta %>';
        });
    </script>
    <% } %>
</body>
</html>
